"""Layer 5 — Evidence Vault (fyp.txt L5).

Append-only, tamper-evident store: SHA-256 hash chain over every record,
RSA signature on the completed bundle, ReportLab PDF export with a Section
90A certificate template. Original contribution — absent from both frameworks.
"""
